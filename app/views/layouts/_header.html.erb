  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%= link_to image_tag("logo.gif", size: "90x90",alt: "Edit Entry", class: "navbar-brand"), pages_path %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Main page", pages_path %>
        </li>
        <li class="nav-item">
          <%= link_to "About me", about_pages_path %>
        </li>
        <% unless user_signed_in? %>
        <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path %></li>
        <li class="nav-item"><%= link_to "Sign In", new_user_session_path %></li>
        <% else %>
        <li class="nav-item"><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
        <% end %>
      </ul>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse"
      data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
      aria-expanded="false" aria-laber="Toggle navigation ">
      <span class="navbar-toggler-icon"></span>
    </button>

      <p id='user-greeting'><%= ("Welcome "+ current_user.email) if user_signed_in? %></p>

  </nav>
<div id="fixfloat"></div>
